# 课堂作业实践记录：使用 AI 辅助完成复利计算程序

> 本文档仅记录目前已真实完成的内容，后续步骤完成后另行补充。

## 1. 作业目标

使用 AI 辅助编写一个复利计算程序，并对比"明确指令"与"模糊指令"两种提问方式的差异。

## 2. 第一次明确指令的原文

> 用 Python 帮我编写一个最基础的复利计算程序。
>
> 要求：
> 1. 本金为 10000 元；
> 2. 年利率为 5%；
> 3. 存款年限为 3 年；
> 4. 每年复利 1 次；
> 5. 使用复利公式：最终金额 = 本金 × (1 + 年利率) ^ 年数；
> 6. 输出最终本息和，并保留两位小数；
> 7. 代码中添加必要的中文注释；
> 8. 请直接修改当前项目中的 compound_interest.py。

## 3. AI 生成结果

AI 直接修改了 `compound_interest.py`，核心内容如下：

```python
principal = 10000   # 本金：10000 元
rate = 0.05         # 年利率：5%，写成小数 0.05
years = 3           # 存款年限：3 年

# 复利计算：每年本金变为上一年的 (1 + 年利率) 倍，共滚存 3 年
final_amount = principal * (1 + rate) ** years

# 输出最终本息和，保留两位小数
print(f"最终本息和：{final_amount:.2f} 元")
```

说明：在此之前 AI 曾生成过一个带"每年复利次数 n"参数的版本；由于本次指令明确要求"最基础"并指定了公式 `(1 + 年利率) ^ 年数`，AI 按指令将代码简化为不含 n 参数的基础版本。

## 4. 人工验证

- 运行 `python compound_interest.py`，实际输出：

  ```
  最终本息和：11576.25 元
  ```

- 人工核验：10000 × (1 + 0.05)³ = 10000 × 1.157625 = 11576.25 元，与程序输出一致，验证通过。

## 5. 模糊指令实验

原指令原文：

> 写个复利计算器，放到 bad_prompt_test.py 里。

## 6. AI 在需求不明确时的自行设计

由于指令未说明具体参数和交互方式，AI 自行做了以下设计：

- 增加了用户输入功能（`input()`），运行时需依次输入本金、年利率、存款年限；
- 增加了"每年复利次数 n"这一参数（指令中并未要求）；
- 约定年利率按百分数输入（如输入 5 表示 5%，内部除以 100 转为小数）；
- 自行决定保留两位小数输出。

## 7. bad_prompt_test.py 验证

测试时输入 10000、5、3、1，实际输出：

```
最终本息和：11576.25 元
```

与固定参数版本结果一致。

## 8. 当前阶段体会

- **明确指令**能够减少 AI 自行假设，使生成结果更可控：指定公式、参数和文件名后，AI 生成的代码与要求完全一致。
- **模糊指令**即使可能得到正确结果，也可能让 AI 自行决定需求（如本次 AI 自行增加了交互输入和复利次数参数），结果未必符合真实意图，需要事后核对甚至返工。

---

# 第二阶段：升级为交互版复利计算器

> 本阶段仅记录已真实完成的内容，后续步骤完成后另行补充。

## 9. 第二阶段任务

在现有 `compound_interest.py` 基础上升级为交互版复利计算器（未新建 Python 文件）：

- 允许用户输入本金和年利率，年利率直接输入百分数（如输入 5 表示 5%）；
- 存款年限暂时固定为 3 年，每年复利 1 次；
- 增加输入校验：本金必须大于 0，年利率不能为负数；
- 输入负利率时提示"年利率不能为负数"，且不进行复利计算。

## 10. 升级后的核心逻辑

```python
principal = float(input("请输入本金（元）："))               # 本金 P
rate = float(input("请输入年利率（如 5 表示 5%）：")) / 100   # 百分数转小数

years = 3  # 存款年限固定为 3 年

if principal <= 0:
    print("本金必须大于 0")
elif rate < 0:
    print("年利率不能为负数")
else:
    final_amount = principal * (1 + rate) ** years
    print(f"最终本息和：{final_amount:.2f} 元")
```

## 11. 第二阶段人工验证

| 输入（本金 / 年利率） | 实际输出 |
| --- | --- |
| 10000 / 5 | 最终本息和：11576.25 元 |
| 10000 / -5 | 年利率不能为负数（未进行复利计算） |

- 10000 × (1 + 0.05)³ = 11576.25 元，与程序输出一致，验证通过；
- 负利率输入被正确拦截，程序按预期提示且未计算。

## 12. 本阶段 Git 提交说明

对应 Git 提交说明：`升级为交互版并增加输入校验`（此处仅记录说明文字，提交操作尚未执行）。

---

# 第三阶段：复利与单利对比

> 本阶段仅记录已真实完成的内容，后续步骤完成后另行补充。

## 13. 第三阶段任务

在保留用户输入本金、年利率及输入校验功能的基础上，为 `compound_interest.py` 增加复利与单利对比功能：

- 分别计算 10、20、30 年的复利终值、单利终值以及两者差额；
- 复利公式：本金 × (1 + 年利率) ^ 年数；
- 单利公式：本金 × (1 + 年利率 × 年数)；
- 以表格形式输出，便于直观对比；
- 暂不制作图形界面，未修改 `bad_prompt_test.py` 与 `progress.md` 之外的其他文件。

## 14. 第三阶段核心逻辑

```python
for years in (10, 20, 30):
    compound = principal * (1 + rate) ** years   # 复利终值：利息滚存
    simple = principal * (1 + rate * years)      # 单利终值：利息不滚存
    diff = compound - simple                     # 差额：复利比单利多出的收益
    print(f"{years:<10}{compound:<20.2f}{simple:<20.2f}{diff:.2f}")
```

## 15. 第三阶段测试数据

输入本金 10000 元、年利率 5%，实际输出如下：

| 年数 | 复利终值（元） | 单利终值（元） | 差额（元） |
| --- | --- | --- | --- |
| 10 | 16288.95 | 15000.00 | 1288.95 |
| 20 | 26532.98 | 20000.00 | 6532.98 |
| 30 | 43219.42 | 25000.00 | 18219.42 |

人工核验（以 30 年为例）：

- 复利：10000 × 1.05³⁰ = 10000 × 4.321942 = 43219.42 元；
- 单利：10000 × (1 + 0.05 × 30) = 10000 × 2.5 = 25000.00 元；
- 差额：43219.42 − 25000.00 = 18219.42 元，与程序输出一致。

观察：差额随年限增长明显加速（10 年差 1288.95 元，30 年扩大到 18219.42 元），体现复利的指数增长效应。

同时验证输入校验仍生效：输入本金 10000、年利率 -5 时，程序提示"年利率不能为负数"且未进行计算。

---

# 第四阶段：复利与单利增长曲线可视化

> 本阶段仅记录已真实完成的内容，后续步骤完成后另行补充。

## 16. 第四阶段任务

在 `compound_interest_gui.py` 中引入 matplotlib，在现有图形界面（本金/年利率输入框、计算按钮、10/20/30 年对比表格、输入校验）基础上新增增长曲线图：

- 横轴为年份（0～30 年），纵轴为金额（元）；
- 分别绘制复利与单利两条曲线；
- 保留输入校验与对比表格，不加入复杂美化。

实现中为支持绘图安装了 matplotlib 3.11.2（含 numpy 等依赖），并配置中文字体以正常显示坐标轴标签。

## 17. 第四阶段核心逻辑

```python
def calculate_series(principal, rate, max_years=30):
    """生成 0~max_years 每一年的复利与单利序列，用于绘制曲线"""
    years = list(range(0, max_years + 1))
    compound = [principal * (1 + rate) ** y for y in years]   # 复利曲线
    simple = [principal * (1 + rate * y) for y in years]     # 单利曲线
    return years, compound, simple

def plot_curves(fig, ax, canvas, principal, rate):
    ax.clear()
    years, compound, simple = calculate_series(principal, rate)
    ax.plot(years, compound, label="复利", color="tab:red")
    ax.plot(years, simple, label="单利", color="tab:blue")
    ax.set_xlabel("年份"); ax.set_ylabel("金额（元）")
    ax.legend(); ax.grid(True, linestyle="--", alpha=0.5)
    fig.tight_layout(); canvas.draw()
```

## 18. 第四阶段测试数据与观察

输入本金 10000 元、年利率 5%，10/20/30 年结果如下（与程序输出一致）：

| 年数 | 复利终值（元） | 单利终值（元） | 差额（元） |
| --- | --- | --- | --- |
| 10 | 16288.95 | 15000.00 | 1288.95 |
| 20 | 26532.98 | 20000.00 | 6532.98 |
| 30 | 43219.42 | 25000.00 | 18219.42 |

观察：随着年限增加，复利与单利的差距逐渐扩大——10 年相差 1288.95 元，20 年扩大到 6532.98 元，30 年达到 18219.42 元。曲线图中复利（指数增长）随时间上翘、与单利（线性增长）的开口越来越大，直观体现了复利的指数增长效应。
